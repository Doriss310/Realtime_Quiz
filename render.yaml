services:
  - type: web
    name: my-laravel-app
    env: php # <-- dùng env chứ không phải runtime
    plan: free
    buildCommand: |
      curl -sS https://getcomposer.org/installer | php
      php composer.phar install --no-dev --optimize-autoloader
    startCommand: php -S 0.0.0.0:10000 -t public
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_KEY
        value: base64:QwCOjIWJYTaighgOWwjHxSFqE2JCfFTI6lg8U4OILh8=
      # Khai báo các biến DB, v.v.
      - key: DB_CONNECTION
        value: mysql
      - key: DB_HOST
        value: dpg-d091bcripnbc73fs20rg-a
      - key: DB_PORT
        value: "5432"
      - key: DB_DATABASE
        value: quiz_project
      - key: DB_USERNAME
        value: quiz_project_user
      - key: DB_PASSWORD
        value: hZkrPahkYq0xOl2QtSKx8Wa1zbnZwzzw
      # (tuỳ chọn) bắt buộc phải để Render cài đúng phiên bản PHP:
      - key: PHP_VERSION
        value: "8.0"
